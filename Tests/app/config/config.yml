# SymfonyFrameworkBundle config
framework:
    secret:          secret
    test: ~
    router:          { resource: "%kernel.root_dir%/../../Resources/config/routing.yml" }
    form:            true
    csrf_protection: true
    templating:      { engines: ['twig'] }
    session:
        storage_id: session.storage.filesystem